<Window x:Class="CryptoApp.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="CryptoApp" Height="600" Width="800">
    <Grid Margin="3">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" MinHeight="20"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition/>
            <ColumnDefinition Width="auto"/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.ColumnSpan="3">
            <Button Content="Home" Margin="3" Height="25" Command="{Binding Path=NavigateToHome}"/>
        </StackPanel>
        <Label Grid.Column="0" Grid.Row="1">Search:</Label>
        <TextBox Grid.Column="1" Grid.Row="1" Margin="4,4,4,4" x:Name="SearchBox"/>
        <Button Grid.Column="2" Grid.Row="1" Margin="4,4,4,4" x:Name="SearchButton">Search</Button>
        <ContentControl Grid.Row="3" Grid.ColumnSpan="3" Content="{Binding Path=Navigation.CurrentView}"/>
    </Grid>
</Window>
